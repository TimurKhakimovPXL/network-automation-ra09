# Empty profile used for the ZTP-only baseline. Add a separate profile when a
# class needs preconfigured features.

description: "Empty profile: devices remain at ZTP-bootstrap minimum"

per_device_changes: []
